{
	"name": "dokploy-logs-mcp",
	"version": "1.0.0",
	"description": "MCP server for reading Docker container logs via SSH",
	"type": "module",
	"main": "src/index.ts",
	"scripts": {
		"start": "bun run src/index.ts",
		"dev": "bun --watch run src/index.ts"
	},
	"dependencies": {
		"@modelcontextprotocol/sdk": "^1.25.2"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"typescript": "^5.9.3"
	}
}
